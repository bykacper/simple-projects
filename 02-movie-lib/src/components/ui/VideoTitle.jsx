export default function VideoTitle({ title }) {
    return (
        <div className="absolute bottom-0 left-0 right-0 bg-black/50 text-white text-center py-1 rounded-b">
            {title}
        </div>
    )
}